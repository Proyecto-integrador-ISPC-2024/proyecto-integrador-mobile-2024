<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:targetApi="31">

        <!-- Actividades de la aplicación -->
        <activity
            android:name=".activity_dashboard"
            android:exported="true"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".ui.ContactActivity"
            android:exported="false" />

        <activity
            android:name=".ui.AboutUsActivity"
            android:exported="false" />

        <!-- Seteo como actividad principal la actividad de perfil -->
<!--        <activity-->
<!--            android:name=".ContactActivity"-->
<!--            android:exported="false" />-->
<!--        <activity-->
<!--            android:name=".AboutUs"-->
<!--            android:exported="false" /> -->

<!--        <activity-->
<!--            android:name=".ui.ProfileActivity"-->
<!--            android:exported="true"-->
<!--            android:theme="@style/AppTheme">-->
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->
<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
<!--        </activity>-->

        <!-- Otra actividad de la aplicación -->

        <!-- Seteo como actividad a inicializar la actividad de productos -->
        <activity
            android:name=".activity_dashboard"
            android:exported="true"
            android:theme="@style/AppTheme"/>

        <activity
            android:name=".ui.Home"
            android:exported="false" />
        <activity
            android:name=".ui.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="false" />
        <activity
            android:name=".ui.Register"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.ProductsActivity"
            android:exported="true"
            android:theme="@style/AppTheme" />

    </application>

</manifest>
